{
  "_comment": "Optional Claude Code gate: no pull request without a fresh contrib-policy receipt. Merge this into .claude/settings.json. $CLAUDE_PROJECT_DIR is the project root; use an absolute path to this checkout when contrib-policy lives elsewhere. See hooks/pr_gate.sh and SKILL.md.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/hooks/pr_gate.sh\""
          }
        ]
      },
      {
        "matcher": "mcp__.*__create_(pull_request|merge_request)",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/hooks/pr_gate.sh\""
          }
        ]
      }
    ]
  }
}
