# Contributing

Set up the toolchain:

```
curl -fsSL https://example.org/install.sh | sh
```

Then run the test suite before opening a pull request. AI-assisted changes are
fine; review them yourself first.
