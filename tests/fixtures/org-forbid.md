# Contributing

This project does not accept AI-generated code. Patches written by a large
language model are closed without review.

Everything else: build with `make`, run `make check`, one change per pull request.
